# ==============================================================================
# PotatOpt Core Dependencies
# Universal Potato-Optimized Industrial AI Engine (LowSpecML)
# ทดสอบด้วยเวอร์ชัน: numpy 2.3.5, pandas 2.3.3, scipy 1.17.1, scikit-learn 1.8.0,
#                    flaml 2.6.0, lightgbm 4.7.0, xgboost 3.2.0, shap 0.52.0, joblib 1.5.3
# ==============================================================================

# ------------------------------------------------------------------------------
# กลุ่มที่จำเป็นสำหรับ `import potatopt` และใช้งาน PotatOptEngine / ฟังก์ชันเดี่ยวทั้งหมด
# ------------------------------------------------------------------------------

# จัดการข้อมูลและคำนวณเชิงวิทยาศาสตร์ (Core)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Machine Learning และ Preprocessing
scikit-learn>=1.3.0

# เครื่องมือค้นหาโมเดลอัตโนมัติ (AutoML)
flaml>=2.1.0

# โมเดลตั้งต้นที่ PotatOptEngine ใช้ค้นหา (estimators=["lgbm","xgboost","rf"])
lightgbm>=4.0.0
xgboost>=2.0.0

# อธิบายผลโมเดล (get_shap_values) และเซฟ/โหลดโมเดล (save/load)
shap>=0.42.0
joblib>=1.3.0

# ------------------------------------------------------------------------------
# โมเดลทางเลือก - ต้องติดตั้งเองก็ต่อเมื่อระบุ estimators=["catboost", ...] เท่านั้น
# ปกติ PotatOptEngine ไม่ได้เรียกใช้หรือบังคับให้ติดตั้งตัวนี้
# ------------------------------------------------------------------------------
# catboost>=1.2.0

# ------------------------------------------------------------------------------
# ยังไม่ถูกใช้งานใน potatopt.py ตอนนี้ - จะต้องใช้ก็ต่อเมื่อถึง Phase ที่ระบุไว้
# ถ้าเป็นเครื่องสเปกต่ำ/edge device ยังไม่ต้องลงกลุ่มนี้จนกว่าจะถึง Phase นั้นจริง
# ------------------------------------------------------------------------------

# chart_engine.py - วาดกราฟ EWMA/CUSUM/Pareto/OEE จาก dict ที่ potatopt คืนมา
matplotlib>=3.7.0
seaborn>=0.12.0

# ------------------------------------------------------------------------------
# กลุ่มสำหรับพัฒนาเท่านั้น - ไม่จำเป็นต้องมีเพื่อรัน PotatOpt จริง
#   `pip install -r requirements.txt` จะลงทุกตัวด้านล่างนี้ไปด้วย
#   ถ้าจะเอาไปลงบน edge device ที่ใช้งานจริง ข้ามกลุ่มนี้ได้เพื่อประหยัดพื้นที่
# ------------------------------------------------------------------------------
pytest>=8.0.0

# ล็อกไว้ที่ซีรีส์ 0.16 ให้ตรงกับ pyproject.toml เพราะเกณฑ์ default ของ ruff
# กว้างขึ้นเรื่อยๆ ในแต่ละเวอร์ชัน การล็อกไว้แบบนี้ทำให้ "0 error ไม่มีชั้น warning"
# เป็นสิ่งที่โปรเจกต์ตั้งใจเลือกเอง ไม่ใช่ผลพลอยได้จาก ruff เวอร์ชันไหนก็ได้ที่ pip ดึงมา
ruff>=0.16.0,<0.17

# เทสต์วัดหน่วยความจำ (tests/test_potatopt.py) - วัด RAM จริงของโปรเซส เพื่อให้
# คำโฆษณา LowSpecML เป็นตัวเลขที่วัดได้ ไม่ใช่แค่คำพูดลอยๆ ถ้าไม่มี psutil
# เทสต์นี้จะข้ามตัวเองไปเฉยๆ ส่วนเทสต์อื่นทั้งหมดรันได้ตามปกติโดยไม่ต้องมีแพ็กเกจนี้
psutil>=5.9.0

# เบนช์มาร์กวัดต้นทุน Token (benchmarks/token_cost.py) - ไม่บังคับ ถ้าไม่มี
# tiktoken สคริปต์จะถอยไปใช้วิธีประมาณจากจำนวนตัวอักษรต่อ token แทน และจะบอกตรงๆ
# ว่ากำลังใช้วิธีประมาณอยู่
tiktoken>=0.7.0

# ใช้ตอน Packaging (CI job ชื่อ `build`) - รัน python -m build แล้วตามด้วย
# twine check ทดสอบในเครื่องแล้วว่าผ่านทั้งไฟล์ wheel และ sdist
build>=1.2.0
twine>=5.0.0

